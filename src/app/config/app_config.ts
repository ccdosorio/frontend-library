import { AppConfig } from '@models';

export const appConfig: AppConfig = {
    colorTheme: 'default',
    layout: {
        generic_container: {
            visible: true
        },
        sidenav: {
            visible: false
        },
        toolbar: {
            visible: true
        }
    }
};
