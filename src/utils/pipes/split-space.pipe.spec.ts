import { SplitSpacePipe } from './split-space.pipe';

describe('SplitSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
